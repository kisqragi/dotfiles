# prompt
PROMPT='[%d]# '
RPROMPT=''

# history
HISTFILE=~/.zsh_history
HISTSIZE=10000
SAVEHIST=10000

# alias configuration
alias cp="cp -ri"
alias ls='ls -aFG'
alias l='ls'
alias la='ls -a'
alias ll='ls -l'
alias rm='rm -ri'
alias mv='mv -i'


